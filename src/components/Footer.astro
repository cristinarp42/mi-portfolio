<footer class="w-full px-4 sm:px-8 py-20 md:py-8 border-t border-stone-200 flex flex-col md:flex-row items-center justify-between gap-12 md:gap-4">

  <!-- BLOQUE CENTRAL (CTA + FRASES RESPONSIVE) -->
  <div class="order-1 md:order-2 flex flex-col md:flex-row items-center justify-center gap-4 md:gap-3 w-full md:w-auto">
    
    <!-- Texto Móvil (ARRIBA en móvil) / Texto Desktop (A LA IZQUIERDA en desktop) -->
    <div class="order-1 text-center md:text-right space-y-1 md:space-y-0 text-stone-600 md:text-stone-500 text-sm md:text-xs leading-relaxed md:italic">
      <!-- Versión Móvil (Larga, 2 líneas) -->
      <p class="block md:hidden">Si has llegado hasta aquí leyendo,</p>
      <p class="block md:hidden">oficialmente <strong class="font-bold text-stone-900">te llevé al huerto.</strong></p>
      
      <!-- Versión Desktop (Corta, 1 línea a la izquierda del botón) -->
      <p class="hidden md:block whitespace-nowrap">
        Admitámoslo: <strong class="font-semibold text-stone-800">te llevé al huerto.</strong>
      </p>
    </div>

    <!-- Botón de copiar email (ABAJO en móvil / A LA DERECHA en desktop) -->
    <button
      id="email-copy-btn"
      class="order-2 group inline-flex items-center justify-center gap-3 md:gap-2.5 px-7 md:px-5 py-3.5 md:py-2.5 rounded-full border border-stone-300 hover:border-[var(--color-accent)] hover:bg-[var(--color-accent)]/5 transition-all cursor-pointer w-full sm:w-auto flex-none"
      data-email="cristinarp42@gmail.com"
      aria-label="Copiar correo electrónico"
    >
      <span class="text-sm md:text-xs font-semibold text-stone-800 group-hover:text-[var(--color-accent)] transition-colors whitespace-nowrap">
        cristinarp42@gmail.com
      </span>
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="text-stone-400 group-hover:text-[var(--color-accent)] transition-colors flex-none">
        <rect x="9" y="9" width="12" height="12" rx="2"/>
        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
      </svg>
    </button>

  </div>

  <!-- EXTREMOS: Redes y Copyright (Fila inferior en móvil, extremos en desktop) -->
  <div class="order-2 md:order-1 flex items-center justify-between w-full md:w-auto md:contents">
    
    <!-- IZQUIERDA: Iconos redes -->
    <div class="flex items-center gap-2 flex-none">
      <a href="https://github.com/tuusuario" aria-label="GitHub" class="w-9 h-9 md:w-8 md:h-8 border border-stone-300 rounded-lg md:rounded-md flex items-center justify-center text-stone-600 hover:bg-[var(--color-accent)] hover:border-[var(--color-accent)] hover:text-white transition-colors">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M9 19c-4.3 1.4-4.3-2.5-6-3m12 5v-3.5c0-1 .1-1.4-.5-2 2.8-.3 5.5-1.4 5.5-6a4.6 4.6 0 0 0-1.3-3.2 4.2 4.2 0 0 0-.1-3.2s-1.1-.3-3.5 1.3a12.3 12.3 0 0 0-6.2 0C6.5 2.8 5.4 3.1 5.4 3.1a4.2 4.2 0 0 0-.1 3.2A4.6 4.6 0 0 0 4 9.5c0 4.6 2.7 5.7 5.5 6-.6.6-.6 1.2-.5 2V21"/>
        </svg>
      </a>
      <a href="https://linkedin.com/in/tuusuario" aria-label="LinkedIn" class="w-9 h-9 md:w-8 md:h-8 border border-stone-300 rounded-lg md:rounded-md flex items-center justify-center text-stone-600 hover:bg-[var(--color-accent)] hover:border-[var(--color-accent)] hover:text-white transition-colors">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6Z"/>
          <rect x="2" y="9" width="4" height="12"/>
          <circle cx="4" cy="4" r="2"/>
        </svg>
      </a>
    </div>

    <!-- DERECHA: Copyright -->
    <p class="text-xs text-stone-400 whitespace-nowrap flex-none flex items-center gap-2 md:order-3">
      © 2026 
      <img src="/logo.svg" alt="Logo" class="h-4 w-auto opacity-40" />
    </p>
  </div>

  <!-- Toast Notificación -->
  <div
    id="copy-toast"
    class="fixed bottom-8 left-1/2 -translate-x-1/2 translate-y-4 opacity-0 pointer-events-none transition-all duration-300 bg-stone-900 text-white text-xs sm:text-sm px-4 py-2.5 rounded-full flex items-center gap-2 z-50 shadow-xl"
  >
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-emerald-400">
      <polyline points="20 6 9 17 4 12"/>
    </svg>
    Email copiado al portapapeles
  </div>
</footer>

<script>
  const emailBtn = document.getElementById('email-copy-btn');
  const toast = document.getElementById('copy-toast');

  if (emailBtn && toast) {
    emailBtn.addEventListener('click', async () => {
      const email = emailBtn.getAttribute('data-email');
      if (!email) return;

      try {
        await navigator.clipboard.writeText(email);
      } catch (err) {
        const textarea = document.createElement('textarea');
        textarea.value = email;
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand('copy');
        document.body.removeChild(textarea);
      }

      toast.classList.remove('opacity-0', 'translate-y-4', 'pointer-events-none');
      toast.classList.add('opacity-100', '-translate-y-2');

      setTimeout(() => {
        toast.classList.remove('opacity-100', '-translate-y-2');
        toast.classList.add('opacity-0', 'translate-y-4', 'pointer-events-none');
      }, 2000);
    });
  }
</script>