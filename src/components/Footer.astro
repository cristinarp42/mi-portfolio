<!-- Cierre de sección para móvil -->
<section class="flex w-full flex-col items-center justify-center px-4 pb-6 pt-20 text-center md:hidden sm:px-8 md:pb-3 md:pt-0">
  <div class="space-y-1 text-[10px] leading-relaxed text-stone-600">
    <p>Admitámoslo: <strong class="font-bold not-italic text-stone-900">te llevé al huerto.</strong></p>
  </div>

  <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="mt-0 animate-pulse text-stone-400">
    <path d="M6 9l6 6 6-6"/>
  </svg>
</section>

<!-- Pie de página -->
<footer class="relative w-full border-t border-stone-800 bg-stone-900 px-4 py-5 text-stone-100 sm:px-8 md:py-3">

  <!-- Layout Móvil -->
  <div class="flex flex-col items-center gap-6 pt-5 md:hidden">
    <button
      class="email-copy-btn group inline-flex cursor-pointer items-baseline gap-1.5"
      data-email="hola@crodriguezperez.com"
      aria-label="Copiar correo electrónico"
    >
      <span class="border-b border-stone-200 pb-0.5 text-sm font-semibold text-stone-300 transition-colors group-hover:border-[var(--color-accent)] group-hover:text-[var(--color-accent)]">
        hola@crodriguezperez.com
      </span>
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="flex-none text-stone-300 transition-colors group-hover:text-[var(--color-accent)]">
        <rect x="9" y="9" width="12" height="12" rx="2"/>
        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
      </svg>
    </button>

    <div class="flex w-full items-center justify-between">
      <div class="flex items-center gap-2">
        <a href="https://www.linkedin.com/in/cristina-rodriguez-56b428433/" aria-label="LinkedIn" class="flex h-8 w-8 items-center justify-center rounded-md border border-stone-700 text-stone-300 transition-colors hover:border-[var(--color-accent)] hover:bg-[var(--color-accent)] hover:text-white">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6Z"/>
            <rect x="2" y="9" width="4" height="12"/>
            <circle cx="4" cy="4" r="2"/>
          </svg>
        </a>
      </div>

      <p class="flex items-center gap-1.5 whitespace-nowrap text-[11px] text-stone-500">
        © 2026
        <img src="/logo.svg" alt="Logo" class="h-3.5 w-auto opacity-50 invert" />
      </p>
    </div>
  </div>

  <!-- Layout Escritorio -->
  <div class="hidden grid-cols-3 items-center md:grid">

    <div class="flex items-center gap-2 justify-self-start">
      <a href="https://www.linkedin.com/in/cristina-rodriguez-56b428433/" aria-label="LinkedIn" class="flex h-8 w-8 items-center justify-center rounded-md border border-stone-700 text-stone-300 transition-colors hover:border-[var(--color-accent)] hover:bg-[var(--color-accent)] hover:text-white">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6Z"/>
          <rect x="2" y="9" width="4" height="12"/>
          <circle cx="4" cy="4" r="2"/>
        </svg>
      </a>
    </div>

    <div class="flex items-center gap-5 justify-self-center">
      <p class="whitespace-nowrap text-[11px] italic text-stone-500">
        Admitámoslo: <strong class="font-semibold not-italic text-stone-300">te llevé al huerto.</strong>
      </p>

      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="flex-none animate-pulse text-stone-500">
        <path d="M9 6l6 6-6 6"/>
      </svg>

      <button
        class="email-copy-btn group inline-flex cursor-pointer items-baseline gap-1.5"
        data-email="hola@crodriguezperez.com"
        aria-label="Copiar correo electrónico"
      >
        <span class="border-b border-stone-200 pb-0.5 text-sm font-semibold text-stone-300 transition-colors group-hover:border-[var(--color-accent)] group-hover:text-[var(--color-accent)]">
          hola@crodriguezperez.com
        </span>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="flex-none text-stone-300 transition-colors group-hover:text-[var(--color-accent)]">
          <rect x="9" y="9" width="12" height="12" rx="2"/>
          <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
        </svg>
      </button>
    </div>

    <p class="flex items-center gap-1.5 whitespace-nowrap text-[11px] text-stone-500 justify-self-end">
      © 2026
      <img src="/logo.svg" alt="Logo" class="h-3.5 w-auto opacity-50 invert" />
    </p>

  </div>

  <!-- Toast Notificación -->
  <div
    id="copy-toast"
    class="pointer-events-none fixed bottom-8 left-1/2 z-50 flex -translate-x-1/2 translate-y-4 items-center gap-2 rounded-full border border-stone-700 bg-stone-800 px-4 py-2 text-xs text-white opacity-0 shadow-xl transition-all duration-300"
  >
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-emerald-400">
      <polyline points="20 6 9 17 4 12"/>
    </svg>
    Email copiado al portapapeles
  </div>
</footer>

<script>
  const emailBtns = document.querySelectorAll('.email-copy-btn');
  const toast = document.getElementById('copy-toast');

  emailBtns.forEach((emailBtn) => {
    emailBtn.addEventListener('click', async () => {
      const email = emailBtn.getAttribute('data-email');
      if (!email || !toast) return;

      try {
        await navigator.clipboard.writeText(email);
      } catch (err) {
        const textarea = document.createElement('textarea');
        textarea.value = email;
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand('copy');
        document.body.removeChild(textarea);
      }

      toast.classList.remove('opacity-0', 'translate-y-4', 'pointer-events-none');
      toast.classList.add('opacity-100', '-translate-y-2');

      setTimeout(() => {
        toast.classList.remove('opacity-100', '-translate-y-2');
        toast.classList.add('opacity-0', 'translate-y-4', 'pointer-events-none');
      }, 2000);
    });
  });
</script>