<!-- FRASE CTA: Fuera del footer en el cuerpo claro de la app -->
<section class="w-full px-4 sm:px-8 pt-20 md:pt-0 pb-6 md:pb-3 flex flex-col items-center justify-center text-center">
  <!-- Frase Móvil (2 líneas) -->
  <div class="block md:hidden space-y-1 text-stone-600 text-[10px] leading-relaxed">
   <p>Admitámoslo: <strong class="font-bold text-stone-900 not-italic">te llevé al huerto.</strong></p>
  </div>

  <!-- Línea divisoria en móvil entre texto y el bloque del footer -->
  <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-stone-400 mt-0 animate-pulse">
    <path d="M6 9l6 6 6-6"/>
  </svg>
</section>

<!-- FOOTER ULTRAFINO -->
<footer class="relative w-full px-4 sm:px-8 py-5 md:py-3 border-t border-stone-800 bg-stone-900 text-stone-100">

  <!-- FRASE FLOTANTE (solo desktop): fuera de la caja, "encima" del footer,
       con flecha curva señalando hacia el botón de email de abajo -->
  <div class="hidden md:flex flex-col items-center absolute left-1/2 -translate-x-1/2 -top-9 z-10 pointer-events-none">
  <p class="text-stone-500 text-[11px] italic tracking-wide whitespace-nowrap">
    Admitámoslo: <strong class="font-semibold text-stone-700 not-italic">te llevé al huerto.</strong>
  </p>
  <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-stone-400 mt-0 animate-pulse">
    <path d="M6 9l6 6 6-6"/>
  </svg>
</div>

  <!-- MÓVIL: apilado, botón arriba, redes/copyright abajo -->
  <div class="flex flex-col items-center gap-6 md:hidden pt-5">
    <button
      class="email-copy-btn group inline-flex items-baseline gap-1.5 cursor-pointer"
      data-email="hola@crodriguezperez.com"
      aria-label="Copiar correo electrónico"
    >
      <span class="text-sm font-semibold text-stone-300 group-hover:text-[var(--color-accent)] transition-colors border-b border-stone-200 group-hover:border-[var(--color-accent)] pb-0.5">
        hola@crodriguezperez.com
      </span>
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="text-stone-300 group-hover:text-[var(--color-accent)] transition-colors flex-none">
        <rect x="9" y="9" width="12" height="12" rx="2"/>
        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
      </svg>
    </button>

    <div class="flex items-center justify-between w-full ">
      <div class="flex items-center gap-2">
        <a href="https://linkedin.com/in/tuusuario" aria-label="LinkedIn" class="w-8 h-8 border border-stone-700 rounded-md flex items-center justify-center text-stone-300 hover:bg-[var(--color-accent)] hover:border-[var(--color-accent)] hover:text-white transition-colors">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6Z"/>
            <rect x="2" y="9" width="4" height="12"/>
            <circle cx="4" cy="4" r="2"/>
          </svg>
        </a>
      </div>

      <p class="text-[11px] text-stone-500 whitespace-nowrap flex items-center gap-1.5">
        © 2026
        <img src="/logo.svg" alt="Logo" class="h-3.5 w-auto opacity-50 invert" />
      </p>
    </div>
  </div>

  <!-- DESKTOP: 3 columnas — redes izquierda / correo centro / copyright derecha -->
  <div class="hidden md:grid grid-cols-3 items-center">

    <div class="flex items-center gap-2 justify-self-start">
      <a href="https://linkedin.com/in/tuusuario" aria-label="LinkedIn" class="w-8 h-8 border border-stone-700 rounded-md flex items-center justify-center text-stone-300 hover:bg-[var(--color-accent)] hover:border-[var(--color-accent)] hover:text-white transition-colors">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6Z"/>
          <rect x="2" y="9" width="4" height="12"/>
          <circle cx="4" cy="4" r="2"/>
        </svg>
      </a>
    </div>

    <div class="justify-self-center">
      <button
        class="email-copy-btn group inline-flex items-baseline gap-1.5 cursor-pointer"
        data-email="hola@crodriguezperez.com"
        aria-label="Copiar correo electrónico"
      >
        <span class="text-sm font-semibold text-stone-300 group-hover:text-[var(--color-accent)] transition-colors border-b border-stone-200 group-hover:border-[var(--color-accent)] pb-0.5">
          hola@crodriguezperez.com
        </span>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="text-stone-300 group-hover:text-[var(--color-accent)] transition-colors flex-none">
          <rect x="9" y="9" width="12" height="12" rx="2"/>
          <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
        </svg>
      </button>
    </div>

    <p class="text-[11px] text-stone-500 whitespace-nowrap flex items-center gap-1.5 justify-self-end">
      © 2026
      <img src="/logo.svg" alt="Logo" class="h-3.5 w-auto opacity-50 invert" />
    </p>

  </div>

  <!-- Toast Notificación -->
  <div
    id="copy-toast"
    class="fixed bottom-8 left-1/2 -translate-x-1/2 translate-y-4 opacity-0 pointer-events-none transition-all duration-300 bg-stone-800 border border-stone-700 text-white text-xs px-4 py-2 rounded-full flex items-center gap-2 z-50 shadow-xl"
  >
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-emerald-400">
      <polyline points="20 6 9 17 4 12"/>
    </svg>
    Email copiado al portapapeles
  </div>
</footer>

<script>
  const emailBtns = document.querySelectorAll('.email-copy-btn');
  const toast = document.getElementById('copy-toast');

  emailBtns.forEach((emailBtn) => {
    emailBtn.addEventListener('click', async () => {
      const email = emailBtn.getAttribute('data-email');
      if (!email || !toast) return;

      try {
        await navigator.clipboard.writeText(email);
      } catch (err) {
        const textarea = document.createElement('textarea');
        textarea.value = email;
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand('copy');
        document.body.removeChild(textarea);
      }

      toast.classList.remove('opacity-0', 'translate-y-4', 'pointer-events-none');
      toast.classList.add('opacity-100', '-translate-y-2');

      setTimeout(() => {
        toast.classList.remove('opacity-100', '-translate-y-2');
        toast.classList.add('opacity-0', 'translate-y-4', 'pointer-events-none');
      }, 2000);
    });
  });
</script>